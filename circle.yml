machine:
  ruby:
    version: 2.4.0
test:
  override:
    - bundle exec jekyll build
deployment:
  prod:
    branch: gh-pages
    commands:
      - aws s3 sync _site/ s3://mobileonly.tools/ --delete --region eu-central-1
